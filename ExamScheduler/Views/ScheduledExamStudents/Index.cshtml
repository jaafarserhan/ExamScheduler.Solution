@model IEnumerable<ExamScheduler.ScheduledExamStudent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ScheduledExam.ScheduledExamID)
        </th>
                    <th>Location</th>
            @Html.DisplayNameFor(model => model.User.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduledExam.ScheduledExamID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ScheduledExamStudentID }) |
                            @Html.ActionLink("Details", "Details", new { id = exam.ScheduledExamStudentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ScheduledExamStudentID })
        </td>
    </tr>
}

</table>
